const Header = () => {
    return (
        <header className="header">
            <div className="logo">
                <span className="course">User List Workshop</span>
            </div>
        </header>
    );
};

export default Header;